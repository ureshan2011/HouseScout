"""HouseScout scrapers: collect Christchurch listings and enrich with LINZ data.

Designed to run from the repo root (so `app` and `scraper` are both importable) or
be invoked by the backend scheduler via app.scraper_hook.
"""
